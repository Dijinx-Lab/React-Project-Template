const assetIcons = {
  profile: "<URL>",
  // Add more as needed
};

const assetImages = {
  heroImage: "<URL>",
  // Add more as needed
};

const assetVideos = {
  heroVideo: "<URL>",
  // Add more as needed
};

export {assetIcons, assetImages, assetVideos};